// src/api/userService.ts
// import axiosClient from "./axiosClient";
// import { User } from "../features/user/types";

// export const userService = {
//   fetchUsers: (): Promise<User[]> => axiosClient.get("/users").then(res => res.data),
//   getUser: (id: number): Promise<User> => axiosClient.get(`/users/${id}`).then(res => res.data),
// };
